name: Minify and Update

on:
  push:
    branches:
      - '*' # Triggers on push to any branch

jobs:
  minify-and-update:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
    - name:  checkout code
      uses: actions/checkout@v4

    - name: ⚙️ Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: 📦 Install dependencies
      run: npm install

    - name: 🚀 Minify JS with Terser
      run: node minify.js

    - name: 📝 Update Chrome Extension script
      run: cp code/source.min.js code/chrome-extension/content.js

    - name: 📝 Update Tampermonkey script
      run: |
        sed -i -e '17r code/source.min.js' -e '17d' code/tampermonkey_userscript/bm-enhanced.min.js
        version=$(grep -oP 'const EXTENSION_VERSION = "\K[^"]+' code/source.js)
        sed -i "4s#^// @version .*#// @version $version#" code/tampermonkey_userscript/bm-enhanced.min.js

    # This is line 44. Notice its indentation matches the step above and below.
    - name: Display final Tampermonkey script
      run: |
        echo "--- Final content of bm-enhanced.min.js before commit ---"
        cat code/tampermonkey_userscript/bm-enhanced.min.js
        echo "---------------------------------------------------------"

    - name: ✨ Commit and push changes
      run: |
        git config --global user.email "github-actions[bot]@users.noreply.github.com"
        git config --global user.name "GitHub Actions"
        git add code/source.min.js code/tampermonkey_userscript/bm-enhanced.min.js code/chrome-extension/content.js
        if ! git diff --staged --quiet; then
          git commit -m "build: Minify JS and update versions [skip ci]"
          git push
        else
          echo "No changes to commit."
        fi